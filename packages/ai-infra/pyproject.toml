[project]
name = "ai-infra"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kelby Madal-Hellmuth", email = "kelby0320@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ai-infra = "ai_infra:main"

[build-system]
requires = ["uv_build>=0.9.8,<0.10.0"]
build-backend = "uv_build"
